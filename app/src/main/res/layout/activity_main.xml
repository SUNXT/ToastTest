<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.youhui.toasttest.MainActivity">

    <EditText
        android:id="@+id/et_toast_count"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="输入toast数量"/>

    <EditText
        android:id="@+id/et_toast_duration"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="输入toast的消失时间"
        android:layout_below="@+id/et_toast_count"/>

    <EditText
        android:id="@+id/et_toast_show_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="显示toast的间隔时间"
        android:layout_below="@id/et_toast_duration"
        />

    <Button
        android:id="@+id/btn_start_toast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="开始显示Toast"
        android:layout_below="@id/et_toast_show_time"/>

</RelativeLayout>
